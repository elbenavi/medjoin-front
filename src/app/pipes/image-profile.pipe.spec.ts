import { ImageProfilePipe } from './image-profile.pipe';

describe('ImageProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
